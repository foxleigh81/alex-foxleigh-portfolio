import React from 'react'
import ReactDOM from 'react-dom'

import SiteNavContainer from '../container/site-nav/SiteNavContainer'

const SiteHeader = (props) => (
  <header>
    This is where the logo goes
    <SiteNavContainer />
  </header>
)

export default SiteHeader
