import React from 'react'
import ReactDOM from 'react-dom'

const SiteHeader = (props) => (
  <header>
    This is where the logo goes
    {/* This is where the site nav will go */}
  </header>
)

export default SiteHeader
